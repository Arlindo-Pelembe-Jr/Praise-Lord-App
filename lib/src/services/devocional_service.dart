import 'package:cloud_firestore/cloud_firestore.dart';

class DevocionalService {
  // CollectionReference devocional =
  //     FirebaseFirestore.instance.collection(ConstantsApp.devocionalCollection);
}
